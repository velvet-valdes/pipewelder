% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipleline_BLS.R
\name{get_bls_chunk}
\alias{get_bls_chunk}
\title{Retrieve a Single BLS Data Chunk}
\usage{
get_bls_chunk(series_id, chunk)
}
\arguments{
\item{series_id}{A character string representing the BLS series ID.}

\item{chunk}{A named list containing \code{start} and \code{end} years, typically produced by \code{\link[=split_year_range]{split_year_range()}}.}
}
\value{
A parsed JSON object (as a list) containing the raw BLS API response.
}
\description{
Makes a single API request to the Bureau of Labor Statistics (BLS) public API
for one series over a limited year range (e.g., a 20-year chunk). Intended for internal use
inside \code{\link[=get_bls_series_chunks]{get_bls_series_chunks()}}, not called directly by users.
}
\details{
Requires the environment variable \code{BLS_KEY} to be set with a valid BLS API key.
}
\examples{
\dontrun{
chunk <- list(start = 2000, end = 2020)
Sys.setenv(BLS_KEY = "your_bls_api_key")
get_bls_chunk("LNS14000000", chunk)
}

}
\seealso{
\code{get_bls_series_chunks()}
}
\keyword{internal}
