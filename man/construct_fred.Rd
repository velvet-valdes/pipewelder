% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline_FRED.R
\name{construct_fred}
\alias{construct_fred}
\title{Retrieve and Construct FRED Series Data}
\usage{
construct_fred(
  sheet,
  start_date = "1950-01-01",
  end_date = Sys.Date(),
  flush = FALSE
)
}
\arguments{
\item{sheet}{A named list that includes an element \code{"FRED"}: a tibble
containing at minimum a \code{"Series ID"} column (character vector of FRED
series IDs), and optionally a \code{"Description"} column for metadata.}

\item{start_date}{Start date for FRED data, in \code{"YYYY"}, \code{"YYYY-MM"},
\code{"YYYY-MM-DD"} format, or as a \code{Date} object. Defaults to \code{"1950-01-01"}.}

\item{end_date}{Optional end date for FRED data. Same format as \code{start_date}.
Defaults to today's date.}

\item{flush}{Logical. If TRUE, clears any existing cached data before making
new API requests. Default is FALSE.}
}
\value{
A named list of tibbles, each corresponding to a FRED series. Each
tibble has columns \code{date} and \code{value}, and includes a \code{"description"}
attribute.
}
\description{
Iterates over a list of FRED series IDs from a tab named \code{"FRED"} in a
\code{sheet} list, retrieving each series using \code{\link[=get_fred]{get_fred()}} and returning a
named list of tibbles. Each tibble includes \code{date} and \code{value} columns, and
is annotated with a \code{"description"} attribute.
}
\examples{
\dontrun{
  # Assuming `sheet` is a list with a "FRED" tibble:
  fred_data <- construct_fred(sheet, start_date = "2000-01-01")
  attr(fred_data$FRED_UNRATE, "description")
}

}
